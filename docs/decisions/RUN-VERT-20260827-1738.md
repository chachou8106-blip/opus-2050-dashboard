# Run vert du 27/08 — 15:36 → 15:39 — vérification complète

Scénario 7051944, run manuel, **statut succès, 76 opérations, 199 s**. Premier run complet
depuis le 26/08 14:15, et le premier avec la chaîne Flash raccordée et le dé-staking réparé.

## Ce qui marche

| Chantier | Vérification |
|---|---|
| **Scénario complet** | 76 opérations sur 76 attendues (79 modules − 3 Binance `[OFF]`) |
| **Les cinq Sages** | Macro, Technique, Risque, Flash, Mémoire — tous `ok` à 15:37:01 |
| **Chaîne Flash** | 4 catalyseurs écrits dans `oracle_flash_intel` à 15:37:00, types corrects (`resultats`, `macro`, `geopolitique`), directions et forces renseignées, routage juste : NVDA → JU seul, FRANCE et MARKET → les 5 archimages |
| **Dé-staking** | 6 lignes avec `verdict = GARDER`, `raison` et `gain_trade_attendu_pct` remplis. Le 26/08 les trois colonnes étaient NULL. |
| **Marées** | proposition EUR-GBP sell (conf 0.7, poids 3 %), 3 positions au livre cible |
| **Archimages** | JU 2 ordres acceptés, SYL 3, GIL 1 — **0 rejeté** |

### Le coupe-circuit a fait son travail

Sur GIL, sous coupe-circuit `drawdown_8pct` depuis le 25/08, deux ordres ont été refusés :

```
AVAXUSD buy  → coupe_circuit_defensif        motif : drawdown 10.23%, déclenché le 25/08 14:19
TQQQ    sell → coupe_circuit_defensif_short   motif : idem
```

C'est la première fois qu'il s'exerce sur le chemin réel du scénario, et pas sur un appel de test.

### L'ordre COIN de 57 766 $ n'est pas une nouvelle prise de risque

GIL a acheté 299,7 COIN pour 57 766 $, et l'ordre est passé malgré le coupe-circuit. C'est
voulu : **c'est le rachat d'une vente à découvert.** GIL avait vendu 300 COIN à découvert le
07/07/2026 pour 49 935 $. Racheter réduit l'exposition — le coupe-circuit laisse toujours passer
ces ordres-là, comme les ventes et les sorties automatiques. La borne de la v41 a fait le reste :
299,7 titres, jamais plus que le short ouvert.

La position se solde sur une perte d'environ 7 800 $.

## Ce qui reste à traiter

**1. Trois `run_id` différents dans un seul run.**

```
flash    20260827-1736
sages    20260827-1737
college  20260827-1738
```

Le scénario met deux minutes et chaque module s'horodate au moment où il écrit. Conséquence :
l'`UPDATE oracle_college_runs … WHERE run_id = v_run_id` de `log_flash_intel` **ne touche jamais
rien** — et de toute façon la ligne du collège n'existe pas encore quand le module 211 s'exécute
(opération 24 contre 59). Les catalyseurs sont bien enregistrés, mais le résumé Flash ne remonte
pas dans `oracle_college_runs`. C'est aussi ce qui a obligé `v_sages_pannes` à rattacher par
fenêtre de temps.

**2. `syl_top_catalyst_ticker` vaut `GLD` à tous les runs, `syl_catalyst_direction` est toujours
NULL.** Ce n'est pas une valeur figée : le module 982 envoie `syl_top_catalyst` =
`{{304.trade_1.ticker}}`, c'est-à-dire **le premier trade de SYL**, pas un catalyseur. SYL trade
GLD à chaque run, d'où la constante. La colonne porte donc un nom qui ment sur son contenu, et
`syl_catalyst_direction` n'est écrite par personne.

**3. L'Alchimiste n'a proposé aucun trade.** Dernière proposition dans
`alchimiste_crypte_propositions` : 26/08 14:17. Le module 10012 a bien tourné (il a écrit les six
verdicts de dé-staking), donc il a renvoyé `propositions: []`. C'est peut-être sa décision, mais
deux runs de suite sans le moindre trade crypto méritent d'être regardés.

**4. `CTX` et `SAGES` vides** — le panneau INPUT du module 305 au run de 13:09 montrait
`CTX=|SAGES=|`. À confirmer sur ce run en ouvrant l'INPUT du module **301 (JU)**.

## Faux positif à ne pas confondre

La Vigie affiche `Planificateur PANNE`, `Alchimiste verdict MUET`, `Trader Marées MUET`. Son
dernier scan date de **15:30**, soit avant le run de 15:36. Le scan de 15:45 remettra tout au
vert. Ce n'est pas une panne, c'est un décalage d'horloge.
