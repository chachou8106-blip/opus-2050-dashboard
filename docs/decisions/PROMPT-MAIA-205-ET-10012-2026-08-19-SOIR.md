# Prompt Maia — les DEUX correctifs manquants : module 205 (run qui meurt) + module 10012 (staking)

> **Statut : PRÊT.** Blueprint relu en direct le 19/08 à 16:34 UTC, deux fois de suite.
> Dernière sauvegarde Make : 12:01. Aucun des deux correctifs n'est dans le scénario.

## Le run de 18:30 est mort à la 19ᵉ opération

```
19/08 16:30:00 UTC  démarrage (18:30 heure française)
19/08 16:31:32 UTC  DataError : Source is not valid JSON  —  module ParseJSON
                    19 opérations sur 76
```

Rien n'a été écrit en base : dernier `oracle_college_runs` à 13:48, derniers Sages à 13:47,
dernier `alc_destake_reco` à 13:48. Pas de Discord, pas d'ordres, pas de staking.

C'est la **quatrième fois** avec exactement la même signature : 18/08 16:31, 19/08 07:04,
19/08 10:13, 19/08 16:31 — toujours 19 opérations, toujours `ParseJSON`.

## Correction d'une affirmation que j'ai faite ce matin

J'ai écrit ce matin que « le correctif max_tokens 2000 tient ». **C'était faux.** Je viens de relire
le module 205 dans le blueprint : `max_tokens` vaut **800**, et la clause LANGUE ne contient pas le
plafond de 200 caractères. Le correctif du 10:26 n'est pas dans le scénario.

Les runs de 11:54, 11:58 et 15:45 sont passés non pas grâce à un correctif, mais parce que la sortie
du Sage Risque est retombée **juste sous** le plafond. C'est exactement ce qui explique
l'intermittence :

| Run | Taille sortie Sage Risque | Résultat |
|---|---|---|
| 11:10 | 965 car. | ✅ |
| 11:55 | 2 405 car. | ✅ |
| 11:59 | 3 009 car. | ✅ (limite frôlée) |
| 13:47 | 2 951 car. | ✅ (limite frôlée) |
| **16:31** | tronquée | ❌ **JSON invalide, run mort** |

À ~3,6 caractères par token, 2 900 caractères ≈ 800 tokens. Le module écrit pile au plafond :
tantôt ça passe, tantôt la phrase est coupée en plein milieu et le module 206 plante.

## État réel des deux modules (relu deux fois, fetch frais)

| Module | Ce qui devrait y être | Ce qui y est |
|---|---|---|
| **205** ⚔️ IRON SENTINEL | `max_tokens: 2000` | **800** ❌ |
| **205** clause LANGUE | plafond « 200 caracteres maximum chacune » | absent ❌ |
| **10012** message user | `STAKING_DELAIS={{ifempty(…)}}` | `STAKING_DELAIS_B64={{base64(…)}}` ❌ |
| **10012** message system | les 3 phrases « texte brut » | anciennes phrases Base64 ❌ |
| **20022** | clé valide + URL alias | ✅ correct, ne pas y toucher |

Les deux pannes sont indépendantes et doivent être corrigées ensemble : le 205 fait **mourir le run**,
le 10012 fait **enregistrer des montants faux** quand le run survit.

## Prompt à envoyer à Maia

Voir le bloc dans la réponse du 19/08 au soir — il combine :

1. Module 205 : `max_tokens` 800 → 2000, et clause LANGUE bornée à 200 caractères par champ texte.
2. Module 10012 : retour au texte brut + les 3 phrases du prompt système + la consigne
   « une ligne par devise, sans omission ».

## Contrôle après le prochain run

- Le run doit dépasser 19 opérations et aller à 76.
- `alc_destake_reco` doit recevoir **7 lignes** : SOL 90,27 · ETH 27,02 · KSM 14,67 · TON 7,91 ·
  ATOM 5,87 · OSMO 4,91 · TRX 0,34 → total 150,99 $ contre 150,98 $ déclarés par Revolut.
- Délais : SOL 3 j · ETH 5 j · KSM 7 j · TON 2 j · ATOM 21 j · OSMO 14 j · TRX 14 j.

## Remarque de fond

Trois correctifs distincts ont été validés puis se sont retrouvés absents du scénario : la clé JWT du
20022 (deux fois, sur deux caractères différents), le `max_tokens` du 205, et le mapping du 10012.
Le point commun est qu'ils ne survivent pas aux sauvegardes successives. Tant que ce n'est pas
compris, **il faut revérifier le blueprint après chaque sauvegarde et avant chaque run**, et ne pas
considérer un correctif comme acquis parce qu'il a été confirmé une fois.
