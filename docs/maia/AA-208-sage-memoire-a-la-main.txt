MODULE 208 — POURQUOI LE RUN DE 20h02 S'EST ARRETE, ET CE QU'ON FAIT
Scenario 7051944. 02/09/2026.

CE QUI S'EST PASSE
------------------
Le run lance a 20:02 (heure de Cannes) s'est arrete au 21e module sur 76.
Make dit, mot pour mot :

  DataError — "Source is not valid JSON." — module ParseJSON

Le 21e module dans l'ordre d'execution est le 208 « DISTILLATION MEMOIRE »,
celui qui lit la reponse du 207 « DEEP MEMORY », le Sage Memoire.

Les quatre premiers Sages ont repondu. Le cinquieme a renvoye quelque chose qui
n'est pas du JSON, et TOUT s'est arrete la : pas d'Archimages, pas d'ordres, pas
de Registre. La derniere ligne en base reste celle du run de 12:22.

Ce n'est PAS une consequence de ta modification de midi : tu as modifie a
12:19:29, le run de 12:19:50 a reussi avec ses 76 operations. Aucune modification
entre celui-la et celui de 20:02. Le scenario n'a pas bouge — c'est la reponse du
Sage Memoire qui a change.


AVANT TOUTE CHOSE : REGARDE L'OUTPUT DU 207
--------------------------------------------
Je n'ai pas acces aux entrees/sorties des modules : l'API Make ne les donne pas.
Toi si. Ouvre l'historique d'execution du run de 20:02, clique sur le module 207
« DEEP MEMORY », panneau OUTPUT, et cherche :

  finishReason

- Si tu lis  finishReason: "MAX_TOKENS"  -> la reponse a ete coupee en cours de
  route, le JSON arrive tronque, et c'est exactement le message d'erreur qu'on a.
  C'est mon hypothese, et la reparation est au point A ci-dessous.
- Si tu lis autre chose (SAFETY, RECITATION, ou du texte en clair au lieu de
  JSON), dis-le-moi : la reparation ne sera pas la meme.

Ne repare rien avant d'avoir regarde. Je ne veux pas te faire modifier un module
sur une supposition.


A) SI C'EST MAX_TOKENS — UN SEUL CHIFFRE A CHANGER, A LA MAIN
--------------------------------------------------------------
Module 207, champ « Body content ». Une seule occurrence, a la fin du corps :

CHERCHE :
"maxOutputTokens":2000

REMPLACE PAR :
"maxOutputTokens":4000

Rien d'autre. C'est un chiffre, pas une expression : aucun risque d'echappement.

TES CONTROLES
  1. Le compteur du champ gagne exactement 0 caractere (2000 et 4000 font
     4 caracteres tous les deux). Il doit afficher 4442 avant ET apres.
  2. Recherche "maxOutputTokens" : une seule occurrence, et elle dit 4000.
  3. Le reste du bloc generationConfig ne bouge pas :
     "temperature":0.01 ... "responseMimeType":"application/json"
     ... "thinkingConfig":{"thinkingBudget":0}

Puis « OK » sur le module, PUIS la disquette sur le scenario.


B) LA VRAIE REPARATION — QU'UN SAGE MUET NE TUE PLUS LE RUN
------------------------------------------------------------
Le point A regle le cas d'aujourd'hui. Il ne regle pas le fond.

Le fond, c'est ceci, et je l'ai verifie sur ton blueprint :

  · Le garde-fou du 208 est  ifempty(...; "{}")  . Il protege du VIDE, pas d'une
    reponse presente mais malformee. Une reponse vide -> "{}" -> le run continue.
    Une reponse tronquee -> elle passe telle quelle -> ParseJSON casse.
  · Les cinq Sages ont exactement le meme montage (202, 204, 206, 208, 210).
    La meme panne peut venir de n'importe lequel.
  · Il y a 14 modules ParseJSON dans le scenario (202, 204, 206, 208, 210, 302,
    304, 306, 402, 9998, 9997, 10010, 10014, 20016).
  · Et AUCUN des 80 modules n'a de gestionnaire d'erreur. Zero. Verifie.

Sur 14 jours, v_sages_pannes compte 2 runs muets pour le Sage Memoire et 5 pour
le Sage Macro, sur 19 runs. Ce n'est pas une panne installee, c'est un alea —
mais un alea qui coute le run entier a chaque fois qu'il tombe.

CE QUE JE PROPOSE, ET C'EST TOI QUI DECIDES :
mettre un gestionnaire d'erreur « Resume » sur les cinq ParseJSON des Sages
(202, 204, 206, 208, 210), qui renvoie un objet vide quand la reponse n'est pas
lisible. Le Sage concerne est alors compte comme muet — ce que v_sages_pannes
sait deja mesurer et afficher — et les quatre autres, les trois Archimages,
l'Alchimiste et le Registre font leur travail.

Dans Make ca se fait au clic droit sur le module -> « Add error handler » ->
« Resume », avec `{}` comme sortie de repli. Ce n'est pas une modification de
texte : je ne peux pas te la livrer en chercher/remplacer, et c'est le genre de
manipulation ou Maia reecrit le module entier — donc ou elle casse les cles.

Dis-moi si tu veux qu'on le fasse, et sur quels modules. Je ne touche a rien
sans ton accord.
