ATTENTION - regles absolues pour cette intervention :
- Scenario 7051944 UNIQUEMENT. Ne touche pas au 6183820.
- UN SEUL module dans ce message : le 303. Ne touche a aucun autre.
- Tu fais TROIS remplacements de texte dans ce seul module, dans l ordre donne.
  Tu ne reformules aucun prompt, tu ne supprimes ni module, ni champ, ni variable, ni connexion.
- Si tu ne trouves pas un texte a chercher EXACTEMENT tel quel, ARRETE-TOI et dis-le.
- N ecris jamais ton raisonnement dans la configuration du module.
- Apres les modifications, verifie que le nombre d accolades { est egal au nombre d accolades }.

MODULE : 303 (LE PROPHETE D ARGENT - SYL)
CHAMP  : jsonStringBodyContent

Meme correctif que sur le module 305 : trois valeurs injectees dans le corps JSON sans etre
nettoyees des guillemets et des antislashs. C est ce genre de trou qui a fait planter SYL le
13 aout avec l erreur Bad escaped character in JSON.

--- REMPLACEMENT 1 sur 3 (CIRCUIT_BREAKERS) ---
Chercher (une seule occurrence, sur une seule ligne) :

{{replace(replace(ifempty(toString(105.data.active_circuit_breakers); emptystring); newline; ); quote; )}}

Remplacer par :

{{replace(replace(replace(replace(ifempty(toString(105.data.active_circuit_breakers); emptystring); backslash; emptystring); quote; emptystring); newline; space); carriagereturn; space)}}

--- REMPLACEMENT 2 sur 3 (MISTAKES) ---
Ici les guillemets ne sont PAS precedes d un antislash. Recopie exactement tel quel.
Chercher (une seule occurrence) :

{{join(map(105.data.brain_states.SYL.mistakes_history; "erreur"); " ## ")}}

Remplacer par :

{{replace(replace(replace(replace(ifempty(toString(join(map(105.data.brain_states.SYL.mistakes_history; "erreur"); " ## ")); emptystring); backslash; emptystring); quote; emptystring); newline; space); carriagereturn; space)}}

--- REMPLACEMENT 3 sur 3 (CATALYSEURS) ---
Chercher (une seule occurrence) :

{{toString(105.data.brain_states.SYL.latest_web_catalysts)}}

Remplacer par :

{{replace(replace(replace(replace(ifempty(toString(105.data.brain_states.SYL.latest_web_catalysts); emptystring); backslash; emptystring); quote; emptystring); newline; space); carriagereturn; space)}}

CONTROLE : apres les trois remplacements, le champ doit faire exactement 6943 caracteres
(il en fait 6570 avant). Ne modifie ni l URL, ni le modele, ni la temperature, ni le prompt
systeme, ni le bloc AUTOCRITIQUE.
