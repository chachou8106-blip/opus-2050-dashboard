PROMPT MAIA — 31/08/2026
Deux modules, deux corrections. Rien d'autre.

============================================================
AVANT TOUTE CHOSE — LES REGLES DE L'INTERVENTION
============================================================

1. Tu interviens UNIQUEMENT dans le scenario 7051944, et UNIQUEMENT sur les
   modules 301 et 10012. Aucun autre module, aucun autre scenario.

2. LES EN-TETES. Ton outil reenregistre le module entier : tu DOIS donc bien
   renvoyer les en-tetes, et c'est normal. Ce qui est interdit, c'est de les
   MODIFIER. Leurs valeurs doivent ressortir identiques, caractere pour
   caractere, exactement telles que tu les as lues. Ne les retape pas de
   memoire, ne les tronque pas, ne les reformate pas, n'y ajoute ni n'enleve
   aucune espace : recopie-les telles quelles.
   C'est le point le plus important de ce message. Lors de precedentes
   interventions, une cle est ressortie alteree sur une soixantaine de
   caracteres EN PLEIN MILIEU de sa valeur, avec un debut et une fin corrects :
   le defaut est donc invisible a l'oeil et ne se voit qu'au compteur.

   CONTROLE OBLIGATOIRE, a faire APRES l'enregistrement et a me rapporter.
   Compte la longueur de chaque valeur d'en-tete. Tu dois retrouver EXACTEMENT :
     module 301   : x-api-key = 108 caracteres
                    anthropic-version = 10 caracteres
                    Content-Type = 16 caracteres
     module 10012 : Authorization = 60 caracteres
                    Content-Type = 16 caracteres
   Si un seul de ces nombres differe, dis-le immediatement et n'enregistre pas
   le scenario. Ne me redonne JAMAIS la valeur d'une cle : sa longueur suffit.

3. Tu ne modifies ni l'URL, ni la methode, ni le parsing de la reponse, ni le
   timeout, ni stopOnHttpError, ni aucun autre reglage du module. Les renvoyer
   inchanges dans l'enregistrement est normal ; les changer ne l'est pas.

4. Tu ne reformules RIEN. Tu ne corriges ni l'orthographe, ni les accents, ni la
   ponctuation, ni la casse. Les textes ci-dessous sont volontairement sans
   accents par endroits : laisse-les tels quels.

5. Dans le corps de la requete, tu fais UNIQUEMENT les remplacements de chaine
   listes plus bas, caractere pour caractere. Tout le reste du corps est
   reenregistre a l'identique. Chacune des chaines "AVANT" apparait EXACTEMENT
   UNE FOIS dans le champ concerne : c'est verifie. Si tu en trouves zero ou
   plusieurs, tu N'APPLIQUES RIEN sur ce module et tu le signales.

6. Les guillemets a l'interieur d'une expression {{ ... }} restent NUS. Tu
   n'ajoutes aucun antislash dans une expression {{ ... }}. Apres ton
   intervention, aucune expression {{ ... }} ne doit contenir d'antislash.
   En revanche, les antislashs deja presents AILLEURS dans le champ (comme dans
   \"raison\" ci-dessous) font partie du texte : tu les gardes a l'identique.

============================================================
MODULE 301 — L'ARCHIMAGE SOUVERAIN JU (appel Anthropic)
============================================================

Champ concerne : le corps de la requete (body).
Longueur actuelle du champ : 7013 caracteres.

UN SEUL remplacement.

AVANT (a chercher exactement, une seule occurrence) :
confidence n'est JAMAIS 0 quand des donnees existent : plancher a 4, calibre entre 4 et 8.

APRES (a mettre a la place) :
confidence est un entier de 0 a 100, meme echelle que SYL et GIL. Tu la calcules toi-meme a partir de tes donnees : aucune valeur, aucun plancher et aucune fourchette ne te sont imposes.

Longueur attendue apres modification : 7109 caracteres (soit +96).

============================================================
MODULE 10012 — L'ALCHIMISTE DE LA CRYPTE (appel Perplexity)
============================================================

Champ concerne : le corps de la requete (body).
Longueur actuelle du champ : 7593 caracteres.

TROIS remplacements, dans cet ordre.

--- Remplacement 1 sur 3 ---

AVANT (une seule occurrence) :
|CTX_B64={{base64(CTX)}}|SAGES_B64={{base64(SAGES)}}|

APRES :
|CTX_B64={{base64(110.value)}}|SAGES_B64={{base64(215.value)}}|

--- Remplacement 2 sur 3 ---

AVANT (une seule occurrence — attention, les antislashs font partie du texte et
doivent etre presents a l'identique) :
\"raison\":\"une phrase courte sans guillemets\"}],\"destake_recommande\"

APRES :
\"raison\":\"une phrase courte sans guillemets\",\"prix_ref\":115200.5}],\"destake_recommande\"

--- Remplacement 3 sur 3 ---

AVANT (une seule occurrence) :
Chaque element de propositions DOIT porter les cinq cles ci-dessus.

APRES :
Chaque element de propositions DOIT porter les six cles ci-dessus. prix_ref est le cours que tu LIS dans PRIX_REVOLUTX_B64 pour cette paire : si la paire ne s'y trouve pas, elle n'est pas negociable et tu ne la proposes pas.

Longueur attendue apres les trois remplacements : 7782 caracteres (soit +189).

============================================================
POUR FINIR — LES DEUX VALIDATIONS
============================================================

Une seule validation ne suffit pas dans Make :
1. "OK" sur chaque module modifie ;
2. PUIS la disquette (enregistrer) sur le scenario lui-meme.
Sans la deuxieme, le blueprint ne bouge pas.

Puis reponds en donnant, pour chaque module :
- le nombre de caracteres du corps de la requete APRES modification ;
- la longueur de chaque valeur d'en-tete, comparee aux nombres de la regle 2.

Ne me redonne aucune valeur de cle, seulement des longueurs.
Ne fais rien d'autre. Ne propose pas d'amelioration.
